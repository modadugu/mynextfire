# Site settings
title: My Next Fire
description: Unfiltered Explorations – A Blog About the Things I Try, Discover, and Share Along the Journey.
baseurl: "/mynextfire" # Required for project sites (matches repo name without .github.io)
url: "https://modadugu.github.io" # Your GitHub username domain

# Build settings
markdown: kramdown
# theme: minima  # Commented out to use custom layouts
permalink: /:year/:month/:day/:title.html

# GitHub Pages compatible plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Safe mode (GitHub Pages uses this automatically)
safe: true

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

